proto init ff-some-feature				# create a new prototype
cd ff-some-feature
proto remote init						# connect to a new github repository
proto watch								# start the grunt task that will progressively compile all the .coffee and .scss files